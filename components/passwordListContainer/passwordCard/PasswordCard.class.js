import {BaseComponent} from "../../baseComponent/BaseComponent.class.js"

export class PasswordCard extends BaseComponent {

   constructor(id, title, accountName, appName, url, password) {
      this.id = id;
      this.title = title;
      this.accountName = accountName;
      this.appName = appName;
      this.url = url;
      this.password = password;
   }

   copyAccountToClipboard() {

   }

   copyPasswordToClipboard() {

   }

   editCardInfo() {

   }

   deleteCard() {

   }

   goToURL() {

   }

}